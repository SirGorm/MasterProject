Recording BIO-signals and depth images"# MasterProject" 

## Creat a virtual enviorment with python 3.10
py -3.10 -m venv venv
venv\Scripts\activate


## Then install requirements
pip install -r requirements.txt

#cd to dir, run main 
python main.py

#to use camera. run pipe_stream_example.exe then connect with view camera




### complete line
py -3.10 -m venv myenv && myenv\Scripts\activate && pip install -r requirements.txt
