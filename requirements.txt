# Requirements for Strength Training Multi-Task Model
# ====================================================
# Install with: pip install -r requirements.txt

# Core ML/DL
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Data handling
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# Optional: for advanced evaluation
scikit-learn>=1.2.0

# Optional: for cross-validation (if using sklearn-based CV)
# Already included in scikit-learn

# Optional: GPU acceleration (uncomment if using CUDA)
# torch with CUDA can be installed via:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
# (adjust cu118 to your CUDA version)
